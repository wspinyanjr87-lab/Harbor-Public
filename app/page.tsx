export default function HarborResetPage() {
  return (
    <main className="min-h-screen bg-family-cream p-6 text-family-ink">
      <section className="mx-auto flex min-h-[70vh] max-w-3xl flex-col justify-center">
        <p className="text-xs font-black uppercase tracking-[0.24em] text-family-berry">Harbor reset</p>
        <h1 className="mt-4 text-5xl font-black leading-tight md:text-7xl">Harbor is being rebuilt.</h1>
        <p className="mt-5 max-w-2xl text-lg leading-8 text-black/60">
          The old interface has been cleared so the next Harbor direction can start clean.
        </p>
      </section>
    </main>
  );
}
